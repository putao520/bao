//! Source-slice satellite crate for bao-mozjs-sys (#42). This crate carries
//! SpiderMonkey sources only; the build-time integration happens in
//! bao-mozjs-sys's build script via the `links` metadata
//! (`cargo:root` -> `DEP_BAO_MOZJS_SRC_*_ROOT`). See that crate for details.
