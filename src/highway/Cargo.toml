[package]
name = "bun_highway"
version.workspace = true
edition.workspace = true
description.workspace = true
license.workspace = true
repository.workspace = true
readme = "README.md"

[lib]
path = "lib.rs"

[lints]
workspace = true

[dependencies]
# @trace REQ-PURE-006
# Pure-Rust SIMD-accelerated byte search — replaces the C++ Google Highway
# dependency. memchr ships SSE2/AVX2/NEON kernels behind a pure-Rust fallback.
memchr.workspace = true
bstr.workspace = true
