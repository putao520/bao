[package]
name = "bun_paths"
# 0.1.1 (2026-08-19): explicit version — breaks workspace inheritance
# so this patch release ships alone. adt_const_params eradicated — crate fully stable-clean + const-generic regression tests.
version = "0.1.11"
edition.workspace = true
description.workspace = true
license.workspace = true
repository.workspace = true
readme = "README.md"

[lib]
path = "lib.rs"

[lints]
workspace = true

[dependencies]
strum.workspace = true
phf.workspace = true
bstr.workspace = true
scopeguard.workspace = true
const_format.workspace = true
enum-map.workspace = true
enumset.workspace = true
libc.workspace = true
bitflags.workspace = true
bytemuck = "1"
bun_alloc.workspace = true
# sibling 精确下限(用户裁决 2026-09-02):path_buffer_pool 消费 bun_core 0.1.13
# 的 asan::ignore_object(2026-09-08 PathBuffer pool sweep 波引入)。
bun_core = { path = "../bun_core", version = "0.1.13" }

thiserror.workspace = true
